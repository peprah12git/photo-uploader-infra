AWSTemplateFormatVersion: '2010-09-09'
Description: Root Stack for Photo Uploader Infrastructure

Parameters:
  GitHubOrg:
    Type: String
    Default: peprah12git
    Description: GitHub organization or username

  GitHubRepo:
    Type: String
    Default: photo-uploader
    Description: GitHub repository name

  DBName:
    Type: String
    Default: photouploader
    Description: Database name

  DBUsername:
    Type: String
    Default: adminuser
    Description: Database master username

  DBPassword:
    Type: String
    NoEcho: true
    Description: Database master password

  CFNTemplatesBucket:
    Type: String
    Description: S3 bucket name for CloudFormation templates

  CreateOIDCProvider:
    Type: String
    Default: 'false'
    AllowedValues:
      - 'true'
      - 'false'
    Description: Set to false if OIDC provider already exists in this account

Resources:
  # ─── ECR Stack ─────────────────────────────────────────────────
  ECRStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/ecr.yml"
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── OIDC Stack ────────────────────────────────────────────────
  OIDCStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/oidc.yml"
      Parameters:
        GitHubOrg: !Ref GitHubOrg
        GitHubRepo: !Ref GitHubRepo
        CreateOIDCProvider: !Ref CreateOIDCProvider
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── VPC Stack ─────────────────────────────────────────────────
  VPCStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/vpc.yml"
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── Security Groups Stack ─────────────────────────────────────
  SecurityGroupsStack:
    Type: AWS::CloudFormation::Stack
    DependsOn: VPCStack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/security-groups.yml"
      Parameters:
        VPCId: !GetAtt VPCStack.Outputs.VPCId
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── VPC Endpoints Stack ───────────────────────────────────────
  VPCEndpointsStack:
    Type: AWS::CloudFormation::Stack
    DependsOn: SecurityGroupsStack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/vpc-endpoints.yml"
      Parameters:
        VPCId: !GetAtt VPCStack.Outputs.VPCId
        PrivateSubnet1Id: !GetAtt VPCStack.Outputs.PrivateSubnet1Id
        PrivateSubnet2Id: !GetAtt VPCStack.Outputs.PrivateSubnet2Id
        PrivateRouteTableId: !GetAtt VPCStack.Outputs.PrivateRouteTableId
        VPCEndpointSecurityGroupId: !GetAtt SecurityGroupsStack.Outputs.VPCEndpointSecurityGroupId
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── S3 and CloudFront Stack ───────────────────────────────────
  S3CloudFrontStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/s3-cloudfront.yml"
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── RDS Stack ─────────────────────────────────────────────────
  RDSStack:
    Type: AWS::CloudFormation::Stack
    DependsOn: SecurityGroupsStack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/rds.yml"
      Parameters:
        VPCId: !GetAtt VPCStack.Outputs.VPCId
        PrivateSubnet1Id: !GetAtt VPCStack.Outputs.PrivateSubnet1Id
        PrivateSubnet2Id: !GetAtt VPCStack.Outputs.PrivateSubnet2Id
        RDSSecurityGroupId: !GetAtt SecurityGroupsStack.Outputs.RDSSecurityGroupId
        DBName: !Ref DBName
        DBUsername: !Ref DBUsername
        DBPassword: !Ref DBPassword
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── ALB Stack ─────────────────────────────────────────────────
  ALBStack:
    Type: AWS::CloudFormation::Stack
    DependsOn: VPCEndpointsStack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/alb.yml"
      Parameters:
        VPCId: !GetAtt VPCStack.Outputs.VPCId
        PublicSubnet1Id: !GetAtt VPCStack.Outputs.PublicSubnet1Id
        PublicSubnet2Id: !GetAtt VPCStack.Outputs.PublicSubnet2Id
        ALBSecurityGroupId: !GetAtt SecurityGroupsStack.Outputs.ALBSecurityGroupId
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── ECS Stack ─────────────────────────────────────────────────
  ECSStack:
    Type: AWS::CloudFormation::Stack
    DependsOn:
      - ALBStack
      - RDSStack
      - S3CloudFrontStack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/ecs.yml"
      Parameters:
        VPCId: !GetAtt VPCStack.Outputs.VPCId
        PrivateSubnet1Id: !GetAtt VPCStack.Outputs.PrivateSubnet1Id
        PrivateSubnet2Id: !GetAtt VPCStack.Outputs.PrivateSubnet2Id
        ECSSecurityGroupId: !GetAtt SecurityGroupsStack.Outputs.ECSSecurityGroupId
        BlueTargetGroupArn: !GetAtt ALBStack.Outputs.BlueTargetGroupArn
        ECRRepositoryUri: !GetAtt ECRStack.Outputs.ECRRepositoryUri
        S3BucketName: !GetAtt S3CloudFrontStack.Outputs.PhotoBucketName
        CloudFrontDomain: !GetAtt S3CloudFrontStack.Outputs.CloudFrontDomainName
        DBHost: !GetAtt RDSStack.Outputs.DBInstanceEndpoint
        DBName: !Ref DBName
        DBUsername: !Ref DBUsername
        DBPassword: !Ref DBPassword
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

  # ─── Pipeline Stack ────────────────────────────────────────────
  PipelineStack:
    Type: AWS::CloudFormation::Stack
    DependsOn: ECSStack
    Properties:
      TemplateURL: !Sub "https://s3.eu-central-1.amazonaws.com/${CFNTemplatesBucket}/templates/pipeline.yml"
      Parameters:
        ECSClusterName: !GetAtt ECSStack.Outputs.ECSClusterName
        ECSServiceName: !GetAtt ECSStack.Outputs.ECSServiceName
        BlueTargetGroupArn: !GetAtt ALBStack.Outputs.BlueTargetGroupArn
        GreenTargetGroupArn: !GetAtt ALBStack.Outputs.GreenTargetGroupArn
        HTTPListenerArn: !GetAtt ALBStack.Outputs.HTTPListenerArn
        TestListenerArn: !GetAtt ALBStack.Outputs.TestListenerArn
        ECRRepositoryName: !GetAtt ECRStack.Outputs.ECRRepositoryName
      Tags:
        - Key: Project
          Value: photo-uploader
        - Key: ManagedBy
          Value: CloudFormation

Outputs:
  ECRRepositoryUri:
    Description: ECR Repository URI
    Value: !GetAtt ECRStack.Outputs.ECRRepositoryUri

  GitHubActionsRoleArn:
    Description: IAM Role ARN for GitHub Actions
    Value: !GetAtt OIDCStack.Outputs.GitHubActionsRoleArn

  ALBDNSName:
    Description: ALB DNS Name
    Value: !GetAtt ALBStack.Outputs.ALBDNSName

  CloudFrontDomainName:
    Description: CloudFront Domain Name
    Value: !GetAtt S3CloudFrontStack.Outputs.CloudFrontDomainName

  DBEndpoint:
    Description: RDS PostgreSQL Endpoint
    Value: !GetAtt RDSStack.Outputs.DBInstanceEndpoint

  ECSClusterName:
    Description: ECS Cluster Name
    Value: !GetAtt ECSStack.Outputs.ECSClusterName

  CodePipelineName:
    Description: CodePipeline Name
    Value: !GetAtt PipelineStack.Outputs.CodePipelineName

  GitHubActionsInfraRoleArn:
    Description: IAM Role ARN for GitHub Actions Infra Repo
    Value: !GetAtt OIDCStack.Outputs.GitHubActionsInfraRoleArn